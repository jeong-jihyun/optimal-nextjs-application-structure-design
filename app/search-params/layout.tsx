import { Metadata } from "next";

export const metadata: Metadata = {
    title: '레벨업 Next.js: URL 처리',
  };
  export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
  }